#include<stdio.h>
main()
{
	int n,a[40],i,r;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	  r=a[0];
	  for(i=0;i<n;i++)
		{
			if(r!=a[i])
			{
				printf("unique");
				break;
			}
		}	
	  if(r==a[n-1])
	  {
	  	printf("duplicate");
	  }
}
